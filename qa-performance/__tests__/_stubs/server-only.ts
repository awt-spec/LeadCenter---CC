// Stub de `server-only` para tests en Node.
// `server-only` es un guard que solo existe para que el bundler de Next
// rompa el build si código server se cuela en client. En tests de Node
// es un noop.
export {};
