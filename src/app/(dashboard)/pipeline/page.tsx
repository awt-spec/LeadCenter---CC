import { PlaceholderPage } from '@/components/shared/placeholder-page';

export const metadata = { title: 'Pipeline' };

export default function PipelinePage() {
  return (
    <PlaceholderPage
      title="Pipeline"
      description="Vista Kanban con los stages comerciales."
    />
  );
}
